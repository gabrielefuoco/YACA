// Default catalog configurations (loaded from backend via window.DEFAULT_CATALOGS)
export const defaultCatalogs = window.DEFAULT_CATALOGS || [];

// Genre Constants (loaded from backend via window.MOVIE_GENRES and window.SERIES_GENRES)
export const MOVIE_GENRES = window.MOVIE_GENRES || [];
export const SERIES_GENRES = window.SERIES_GENRES || [];
