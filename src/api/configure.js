const configureHandler = require('./configure/index');

/**
 * Configure Handler: Gestisce il salvataggio della configurazione utente.
 * Refactored into a modular structure in ./configure/
 */
module.exports = configureHandler;
